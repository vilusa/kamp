 <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: name %>
    <% end -%>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>
 
<h2>Comments</h2>
<%= render(@article.comments)  || 'No Comment'%>
# Eğer comments yoksa no comments yazar ekrana
 
<h2>Add a comment:</h2>
<%= render 'comments/form' %>
 
<%= link_to 'Edit', edit_kamp_article_path(@article) %> |
<%= link_to 'Back', kamp_articles_path %>